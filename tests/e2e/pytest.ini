[pytest]
markers =
    e2e: End-to-end tests that require the application to be running in Docker Compose. These tests will be skipped if Docker Compose is not available or if the application is not running.    